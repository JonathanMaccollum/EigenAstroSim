
<!-- StarFieldView.xaml -->
<UserControl x:Class="EigenAstroSim.UI.Views.StarFieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    
    <Grid>
        <!-- Star Field Display -->
        <Border BorderBrush="#444444" BorderThickness="1" Margin="10">
            <Grid>
                <!-- Default text when no image is available -->
                <TextBlock Text="Star Field Display (Waiting for image...)" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="{Binding HasImage.Value, Converter={StaticResource InverseBoolConverter}, ConverterParameter=InverseVisibility}" />
                
                <!-- Image display -->
                <Image Source="{Binding CurrentImage.Value, Converter={StaticResource ByteArrayToImageConverter}}"
                       Stretch="Uniform"
                       Visibility="{Binding HasImage.Value, Converter={StaticResource BoolToVisibilityConverter}}" />
                
                <!-- Overlay with control buttons -->
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Bottom"
                            Margin="10">
                    <Button Content="Generate Trail" 
                            Command="{Binding GenerateSatelliteTrailCommand}"
                            Padding="8,4"
                            Margin="5"
                            Opacity="0.8" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>